version: '3'
services:
  postgres:
    image: postgres
    restart: unless-stopped
    ports:
      - "5442:5432"
    env_file: .env
    environment:
      POSTGRES_DB: paralogs-auth-dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pg-password
    volumes:
      - ./pgdata/auth/dev:/var/lib/postgresql/data

